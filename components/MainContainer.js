import styled from 'styled-components/native';

const MainContainer = styled.View`
  flex: 1;
  align-items: center;
  justify-content: center;
  background-color: #3498db; /* Updated background color to match the theme */
`;

export default MainContainer;
